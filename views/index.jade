extends layout

block content
  h1= title
  p Welcome to #{title}
ul
  each msg in messages.length ? messages : ['There are no new messages']
    li= msg
    //- - for (var i = 0; i < messages.length; ++i) {
//-   li= messages[i]
//- - }